# @page-agent/connect

Simplified local UI for connecting PageAgent and logging in models.

```bash
# from repo root
npm start
```

Keys are stored only in browser `localStorage`. The playground loads PageAgent from the public CDN.
